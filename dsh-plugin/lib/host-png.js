// Compatibility entry point for the DSH adapter. The renderer is shared by
// the DSH and MCP adapters and lives with the studio core.
export { flattenScenePaint, renderEditorPng, renderPaintPng, renderScenePng } from '../../studio/host-png.js'
