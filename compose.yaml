services:
  simulator:
    build: .
    ports:
      - "127.0.0.1:4173:4173"
    environment:
      QXQY_WEB_PASSWORD: ${QXQY_WEB_PASSWORD:?Set QXQY_WEB_PASSWORD before starting}
    volumes:
      - simulator-data:/data
    init: true
    restart: unless-stopped
volumes:
  simulator-data:
